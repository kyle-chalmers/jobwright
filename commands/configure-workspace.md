---
description: Deprecated v1 alias — use /setup instead. Will be removed in 1.0.
---

# /configure-workspace (merged into /setup in the v2 UX release)

Config creation and editing now live in `/setup` (backed by the `jobwright init` wizard). Invoke
the `setup` skill with the same arguments: $ARGUMENTS
