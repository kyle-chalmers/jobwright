---
description: Deprecated v1 alias — use /setup instead. Will be removed in 1.0.
---

# /onboard (renamed in the v2 UX release)

First-time setup is now `/setup`. Invoke the `setup` skill with the same arguments: $ARGUMENTS
