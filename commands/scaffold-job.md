---
description: Deprecated v1 alias — use /start-job instead. Will be removed in 1.0.
---

# /scaffold-job (folded into /start-job in the v2 UX release)

Scaffolding is now Phase 3 of the front door. Invoke the `start-job` skill with the same
arguments: $ARGUMENTS
